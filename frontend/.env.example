# PAROL6 Frontend Environment Variables
# Copy this file to .env.local and configure for your environment

# API URL Configuration
# =====================
# By default, the API URL is automatically detected from the browser's hostname.
# This works for most deployments where frontend and API run on the same machine.
#
# Uncomment and set this variable to override automatic detection:
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Common Deployment Scenarios:
# ----------------------------
#
# 1. Local Development (default):
#    # No configuration needed - uses http://localhost:3001
#
# 2. Same Machine (frontend port 3000, API port 3001):
#    # No configuration needed - auto-detected
#
# 3. Network Access (replace with your IP):
#    NEXT_PUBLIC_API_URL=http://192.168.1.100:3001
#
# 4. Custom Domain/Hostname:
#    NEXT_PUBLIC_API_URL=http://parol6.local:3001
#
# 5. Different Machines (API on separate server):
#    NEXT_PUBLIC_API_URL=http://robot-server.local:3001

# Notes:
# - The NEXT_PUBLIC_ prefix makes this variable available in the browser
# - Always use http:// or https:// prefix
# - The API server must have CORS configured for your frontend URL
# - After changing this file, restart the Next.js dev server
